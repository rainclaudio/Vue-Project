import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      light: {
        primary: '#0e76a8', 
        accent_orange: '#E68523'
      },
      dark: {
        
      },
    },
  },
});
